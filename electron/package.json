{
  "name": "bird-audio-electron",
  "version": "0.1.0",
  "description": "Bird Audio desktop app",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack": "electron-packager . BirdAudio --platform=all --arch=all --out=dist --overwrite"
  },
  "devDependencies": {
    "electron": "^26.0.0"
  }
}
