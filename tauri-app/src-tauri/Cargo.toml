[package]
name = "bird_audio_tauri"
version = "0.1.0"
authors = ["You <you@example.com>"]
edition = "2021"
build = "build.rs"

[dependencies]
tauri = { version = "1.4", features = [] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }

[build-dependencies]
tauri-build = { version = "1.4", features = [] }
